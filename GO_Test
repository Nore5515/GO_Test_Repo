package main

import "fmt"

func add(x, y int) int{
  return x + y
}

func subtract(x, y int) int{
  return x - y
}

func division(x, y intt) 

func main() {
  a := 5
  b := 3
  fmt.Println(add(a,b))
  fmt.Println(subtract(a, b))
}
